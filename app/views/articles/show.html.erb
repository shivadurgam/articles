<p id="notice"><%= notice %></p>

<p>
  <strong><h3> <%= @article.name %></h3></strong> &nbsp by <%= @article.user.email %>
</p>

<p>
  <strong>Description:</strong>
  <%= @article.content %>
</p>
<% if @article.comments.any? %><br /><hr> 
<h4>Comments: </h4><%= @article.comments.count %>&nbsp comments:</p>
          
  <ol type="1"><% @article.comments.each do |comment| %>
           <li><%= comment.name %> &nbsp &nbsp posted <%= time_ago_in_words(comment.created_at) %> ago &nbsp by <%= comment.user.email %> &nbsp &nbsp &nbsp 
                  <% if comment.user.email == current_user.email %> 
                    <%= link_to 'Delete', [comment.article, comment], method: :delete, data: { confirm: 'Are you sure?' } %>
                  <% elsif @article.user.email == current_user.email %>
                    <%= link_to 'Delete', [comment.article, comment], method: :delete, data: { confirm: 'Are you sure?' } %>
                  <% end %></li>
               <% end  %></ol><br />

          <%= render 'comments/form' %>
<% else %>
<p>I dont have comments. please add one..</p>
          <%= render 'comments/form' %>
<% end %>



